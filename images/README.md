# images/ — private local test media

The repository distributes no IBM media or generated SSP volumes. Content
placed here by a developer is ignored by git (`images/*` in `.gitignore`),
apart from this notice and explicitly tracked command-file templates.

The templates contain machine configuration only. Any volume they name must
be supplied privately by the user and is not part of SIM/36.
