#include "Processors/ControlStorage/TaskBlock.h"

namespace sim36::processors::controlstorage {

// The task block is offsets and two inline predicates; this translation unit
// exists so the header's static_asserts are compiled once.

}  // namespace sim36::processors::controlstorage
