#include "Processors/ControlStorage/AllocationQueueElement.h"

namespace sim36::processors::controlstorage {

// The allocation queue element is offsets and bit names only; this
// translation unit exists so the header's static_asserts are compiled once.

}  // namespace sim36::processors::controlstorage
